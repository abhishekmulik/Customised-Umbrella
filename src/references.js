const ColorRowEl = document.querySelector('.color-row')
const imageUploadInputEl = document.getElementById('imageUploadInput');
const uploadButtonEl = document.getElementById('upload-btn');
const previewImageEl = document.getElementById('previewImage')
const umbrellaImgEl = document.getElementById('umbrella-image')
const loaderIconEl = document.getElementById('loader-icon')

export default {
    ColorRowEl,
    imageUploadInputEl,
    uploadButtonEl,
    previewImageEl,
    umbrellaImgEl,
    loaderIconEl
}